[package]
name = "alder-observation"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "The observation application: durable levels reported by dumb observers"

[dependencies]
alder-log = { path = "../alder-log" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
